# Recurring Frustrations Tracker

When something frustrates you more than once, add it here. The smart-boot hook reads this file at session start and surfaces unresolved items proactively.

## How It Works

1. Add a frustration below (one per line, starting with `- `)
2. Smart-boot reads this file every session start
3. Claude sees your frustrations and fixes them proactively
4. When resolved, ~~strikethrough~~ the line (don't delete — it's history)

## Active Frustrations

<!-- Add yours below. Format: - "What keeps happening" — when/where it happens -->

## Resolved

<!-- Move resolved items here with ~~strikethrough~~ -->

---

**The Rule:** If you have to ask for the same thing twice, it's a system failure — not a you failure. Track it here, and the system learns.
